import SimpleTodoList from './SimpleTodoList';

const Lists = () => {
    return (
        <div className="list-card" style={{ padding: '1rem' }}>
            <SimpleTodoList />
        </div>
    );
};

export default Lists;
